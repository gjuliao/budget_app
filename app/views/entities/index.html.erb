<%= render 'partials/navbarentities'  %>

<div class="entities">

  <div class="entity_card_total">
   <p> Total: </p>
    <p> <strong> <%= @entities_sum %> </strong>  </p>
 </div>


  <div id="entities">
    <% @entities.each do |entity| %>
      <%= render entity %>
      <p>
        <% link_to "Show this entity", user_group_entities_path(current_user, @group) %>
      </p>
    <% end %>
  </div>

  <div class="btn-submit-parent">
    <%= link_to "NEW TRANSACTION", new_user_group_entity_path(current_user, @group), class:"btn_submit" %>
  </div>
</div>
